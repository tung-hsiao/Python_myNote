# git

```
git config --list
git config --global user.name "xxx"
git config --global user.mail "xxx@yyy"

git branch dev
git bracn
git checkout dev

git status
git add .
git commit -m "zzz"

git checkout main
git merge dev
git merge dev --no--ff

git reset HEAD^
```